---
label: Form
hide_body: true
display_field: title
fields:
- name: title
  type: text
  config:
    required: true
    max: 255
  label: Title
  default: Contact Form
- name: action
  type: text
  config:
    required: true
  label: Email
  description: What email address do you want the form to send an email to?
- name: fields
  type: blocks
  label: Fields
  template_types:
  - input
  - textarea
  config:
    min: 1
    max: 
  description: What information do you want to collect?
